language: c++

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y qt5-qmake

script: qmake network-clipboard.pro -r -spec linux-g++

notifications:
  email:
    on_success: never
    on_failure: never
